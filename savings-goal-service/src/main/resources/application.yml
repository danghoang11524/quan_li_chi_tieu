server:
  port: 8084

spring:
  application:
    name: savings-goal-service
  datasource:
    url: jdbc:mysql://localhost:3306/expense_savings_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

logging:
  level:
    com.expense.savings: DEBUG
    org.hibernate.SQL: DEBUG
